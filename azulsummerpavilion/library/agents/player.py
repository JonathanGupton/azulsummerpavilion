from abc import ABC
from abc import abstractmethod


class Player(ABC):
    """
    Base class with the required actions necessary to play AzulSummerPavilion.

    Each abstract method represents one type of logic required to be implemented
    by the Player class in order to respond to the actions requested by the game state.
    """

    pass
